# 工具演示视频

各工具卡片与详情页顶部的循环预览视频，默认存放在此目录（`{工具 id}.mp4`）。

## 重新下载

```bash
bash scripts/download-demo-videos.sh
```

素材来自 [Coverr](https://coverr.co/) 免费库。也可自行替换同名 MP4，或修改 `src/lib/tool-demos.ts` 中的远程地址。
